apiVersion: v2
name: elf
home: "https://github.com/elf-io/elf"
# application or library
type: application
# no need to modify this version , CI will auto update it with /VERSION
version: 0.1.0
# This field is informational, and has no impact on chart version calculations .
# Leaving it unquoted can lead to parsing issues in some cases
# no need to modify this version , CI will auto update it with /VERSION
appVersion: "0.1.0"
kubeVersion: ">= 1.16.0-0"
description: elf
sources:
  - "https://github.com/elf-io/elf"
keywords:
  - elf
maintainers:
  - name: elf-io
    url: https://github.com/elf-io
