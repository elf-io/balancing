{{- if (eq .Values.elfController.tls.method "auto") }}
{{- $_ := include "generate-ca-certs" . }}
{{- end }}

apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: {{ .Values.elfController.name | trunc 63 | trimSuffix "-" }}
  annotations:
    {{- if (eq .Values.elfController.tls.method "certmanager") }}
    cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ .Values.elfController.name | trunc 63 | trimSuffix "-" }}-server-certs
    {{- end }}
webhooks:
  - admissionReviewVersions:
      - v1beta1
    clientConfig:
      service:
        name: {{ .Values.elfController.name | trunc 63 | trimSuffix "-" }}
        namespace: {{ .Release.Namespace }}
        path: "/mutate-balancing-elf-io-v1beta1-balancingpolicy"
        port: {{ .Values.elfController.webhookPort }}
      {{- if (eq .Values.elfController.tls.method "provided") }}
      caBundle: {{ .Values.elfController.tls.provided.tlsCa | required "missing tls.provided.tlsCa" }}
      {{- else if (eq .Values.elfController.tls.method "auto") }}
      caBundle: {{ .ca.Cert | b64enc }}
      {{- end }}
    failurePolicy: Fail
    name: balancingpolicy.balancing.elf.io
    rules:
      - apiGroups:
          - balancing.elf.io
        apiVersions:
          - v1beta1
        operations:
          - CREATE
          - UPDATE
          - DELETE
        resources:
          - balancingpolicys
    sideEffects: None
  - admissionReviewVersions:
      - v1beta1
    clientConfig:
      service:
        name: {{ .Values.elfController.name | trunc 63 | trimSuffix "-" }}
        namespace: {{ .Release.Namespace }}
        path: "/mutate-balancing-elf-io-v1beta1-localredirectpolicy"
        port: {{ .Values.elfController.webhookPort }}
      {{- if (eq .Values.elfController.tls.method "provided") }}
      caBundle: {{ .Values.elfController.tls.provided.tlsCa | required "missing tls.provided.tlsCa" }}
      {{- else if (eq .Values.elfController.tls.method "auto") }}
      caBundle: {{ .ca.Cert | b64enc }}
      {{- end }}
    failurePolicy: Fail
    name: localredirectpolicy.balancing.elf.io
    rules:
      - apiGroups:
          - balancing.elf.io
        apiVersions:
          - v1beta1
        operations:
          - CREATE
          - UPDATE
          - DELETE
        resources:
          - localredirectpolicys
    sideEffects: None

---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: {{ .Values.elfController.name | trunc 63 | trimSuffix "-" }}
  annotations:
    {{- if (eq .Values.elfController.tls.method "certmanager") }}
    cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ .Values.elfController.name | trunc 63 | trimSuffix "-" }}-server-certs
    {{- end }}
webhooks:
  - admissionReviewVersions:
      - v1
    clientConfig:
      service:
        name: {{ .Values.elfController.name | trunc 63 | trimSuffix "-" }}
        namespace: {{ .Release.Namespace }}
        path: "/validate-balancing-elf-io-v1beta1-balancingpolicy"
        port: {{ .Values.elfController.webhookPort }}
      {{- if (eq .Values.elfController.tls.method "provided") }}
      caBundle: {{ .Values.elfController.tls.provided.tlsCa | required "missing tls.provided.tlsCa" }}
      {{- else if (eq .Values.elfController.tls.method "auto") }}
      caBundle: {{ .ca.Cert | b64enc }}
      {{- end }}
    failurePolicy: Fail
    name: balancingpolicy.balancing.elf.io
    rules:
      - apiGroups:
          - balancing.elf.io
        apiVersions:
          - v1beta1
        operations:
          - CREATE
          - UPDATE
          - DELETE
        resources:
          - balancingpolicys
    sideEffects: None
  - admissionReviewVersions:
      - v1
    clientConfig:
      service:
        name: {{ .Values.elfController.name | trunc 63 | trimSuffix "-" }}
        namespace: {{ .Release.Namespace }}
        path: "/validate-balancing-elf-io-v1beta1-localredirectpolicy"
        port: {{ .Values.elfController.webhookPort }}
      {{- if (eq .Values.elfController.tls.method "provided") }}
      caBundle: {{ .Values.elfController.tls.provided.tlsCa | required "missing tls.provided.tlsCa" }}
      {{- else if (eq .Values.elfController.tls.method "auto") }}
      caBundle: {{ .ca.Cert | b64enc }}
      {{- end }}
    failurePolicy: Fail
    name: localredirectpolicy.balancing.elf.io
    rules:
      - apiGroups:
          - balancing.elf.io
        apiVersions:
          - v1beta1
        operations:
          - CREATE
          - UPDATE
          - DELETE
        resources:
          - localredirectpolicys
    sideEffects: None

{{- if eq .Values.elfController.tls.method "certmanager" -}}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.elfController.name | trunc 63 | trimSuffix "-" }}-server-certs
  namespace: {{ .Release.Namespace }}
spec:
  issuerRef:
    name: {{ .Values.elfController.tls.certmanager.issuerName | trunc 63 | trimSuffix "-" }}
  secretName: {{ .Values.elfController.tls.secretName | trunc 63 | trimSuffix "-" }}
  commonName: {{ .Values.elfController.name | trunc 63 | trimSuffix "-" }}.{{ .Release.Namespace }}.svc
  dnsNames:
    - {{ .Values.elfController.name | trunc 63 | trimSuffix "-" }}
    - {{ .Values.elfController.name | trunc 63 | trimSuffix "-" }}.{{ .Release.Namespace }}
    - {{ .Values.elfController.name | trunc 63 | trimSuffix "-" }}.{{ .Release.Namespace }}.svc
    - {{ .Values.elfController.name | trunc 63 | trimSuffix "-" }}.{{ .Release.Namespace }}.svc.{{ .Values.global.clusterDnsDomain }}
  {{- range $dns := .Values.elfController.tls.certmanager.extraDnsNames }}
  - {{ $dns | quote }}
  {{- end }}
  {{- if .Values.elfController.tls.certmanager.extraIPAddresses }}
  ipAddresses:
  {{- range $ip := .Values.elfController.tls.certmanager.extraIPAddresses }}
  - {{ $ip | quote }}
  {{- end }}
  {{- end }}
  duration: {{ printf "%dh" (mul .Values.elfController.tls.certmanager.certValidityDuration 24) }}
  {{- end }}

{{- if (eq .Values.elfController.tls.method "provided") }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.elfController.tls.secretName | trunc 63 | trimSuffix "-" }}
  namespace: {{ .Release.Namespace }}
type: kubernetes.io/tls
data:
  ca.crt:  {{ .Values.elfController.tls.provided.tlsCa | required "missing tls.provided.tlsCa" }}
  tls.crt: {{ .Values.elfController.tls.provided.tlsCert | required "missing tls.provided.tlsCert" }}
  tls.key: {{ .Values.elfController.tls.provided.tlsKey  | required "missing tls.provided.tlsKey"  }}
  {{- end }}

{{- if eq .Values.elfController.tls.method "auto" }}
---
{{- $cn := printf "elf.io" }}
{{- $ip := .Values.elfController.tls.auto.extraIpAddresses }}
{{- $dns1 := printf "%s.%s" .Values.elfController.name .Release.Namespace }}
{{- $dns2 := printf "%s.%s.svc" .Values.elfController.name .Release.Namespace }}
{{- $dns3 := printf "%s.%s.svc.%s" .Values.elfController.name .Release.Namespace .Values.global.clusterDnsDomain }}
{{- $dns := prepend .Values.elfController.tls.auto.extraDnsNames $dns1 }}
{{- $dns = prepend $dns $dns2 }}
{{- $dns = prepend $dns $dns3 }}
{{- $dns = prepend $dns $cn }}
{{- $cert := genSignedCert $cn $ip $dns (.Values.elfController.tls.auto.certExpiration | int) .ca }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.elfController.tls.secretName | trunc 63 | trimSuffix "-" }}
  namespace: {{ .Release.Namespace }}
type: kubernetes.io/tls
data:
  ca.crt:  {{ .ca.Cert | b64enc }}
  tls.crt: {{ $cert.Cert | b64enc }}
  tls.key: {{ $cert.Key  | b64enc }}
{{- end }}